# TODO: Add Filters for Price Range and Size

## Steps to Complete

1. **Update index.html**: Add filter section in header with price min/max inputs and size multi-select dropdown.
2. **Update styles.css**: Style the filter elements to be responsive and fit in the header.
3. **Update script.js**:
   - Modify displayDresses() to accept a filtered array.
   - Add event listeners for filter inputs to dynamically update displayed dresses.
   - Implement filtering logic for price range and selected sizes.
4. **Test Filtering**: Verify filters work with different ranges and sizes, and reset to show all when cleared.

## Completed Steps

- [x] Update index.html: Added filter section in header with price min/max inputs and size multi-select dropdown.
- [x] Update styles.css: Styled the filter elements to be responsive and fit in the header.
- [x] Update script.js: Modified displayDresses() to accept a filtered array, added event listeners for filter inputs to dynamically update displayed dresses, implemented filtering logic for price range and selected sizes.
- [x] Test Filtering: Verified filters work with different ranges and sizes, and reset to show all when cleared.
